apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: sre-copilot
  labels:
    app: api-gateway
spec:
  type: ClusterIP
  ports:
    - port: 8500
      targetPort: 8500
  selector:
    app: api-gateway
