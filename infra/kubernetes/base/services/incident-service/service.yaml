apiVersion: v1
kind: Service
metadata:
  name: incident-service
  namespace: sre-copilot
  labels:
    app: incident-service
spec:
  type: ClusterIP
  ports:
    - port: 8502
      targetPort: 8502
  selector:
    app: incident-service
